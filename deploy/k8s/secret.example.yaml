# Fabricated values. The warehouse token here is the same invented string that is
# hardcoded in WarehouseClient and baked into the Dockerfile, which is the point: the
# secret exists as a Kubernetes object and the code ignores it.
#
# Copy to secret.yaml and fill in real values if you ever point this at something real.
apiVersion: v1
kind: Secret
metadata:
  name: ratings-warehouse
  namespace: northgate
type: Opaque
stringData:
  NORTHGATE_WAREHOUSE_USER: svc_ratings
  NORTHGATE_WAREHOUSE_PASSWORD: Wareh0use-2019!
  NORTHGATE_WAREHOUSE_API_TOKEN: ngw_live_8f3c21bb94d24e7ba0c15ee7d3f10a92
